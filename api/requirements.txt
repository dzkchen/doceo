# Pinned just enough for reproducibility. The .venv was set up by hand;
# this file documents what's needed to recreate it from scratch.

fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# Step 8: MIDI ingest + score render
pretty_midi>=0.2.11
music21>=9.1

# Step 10: audio-to-MIDI transcription
basic-pitch[onnx]>=0.4.0
# basic-pitch -> resampy still imports pkg_resources, which setuptools 81+ removed.
setuptools<81

# Step 19: tutor pipeline
anthropic>=0.97.0
elevenlabs>=2.20.0
